# --- Discord ---------------------------------------------------------------
discord.py[voice]>=2.3.0
PyNaCl>=1.5.0            # chiffrement voix (requis par [voice])

# --- Fournisseurs LLM ------------------------------------------------------
# Seul Cerebras a besoin d'un SDK. Groq et Gemini sont appelés en HTTP brut via
# aiohttp (endpoints OpenAI-compatible / natif Gemini) : pas de paquet en plus,
# pas de conflit de versions, et le safetySettings de Gemini reste accessible.
cerebras-cloud-sdk>=1.0.0

# --- Divers ----------------------------------------------------------------
python-dotenv>=1.0.0
yt-dlp>=2024.1.0         # à garder à jour : YouTube casse régulièrement l'extraction
aiohttp>=3.9.0           # keep-alive, panneau admin, ET les appels Groq/Gemini